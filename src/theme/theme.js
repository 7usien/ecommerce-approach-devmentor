import { createTheme } from '@mui/material';

export const themeOptions = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#457285',
    },
    secondary: {
      main: '#f50057',
    },
  },

  palette: {
    type: 'dark',
    primary: {
      main: '#457285',
    },
    secondary: {
      main: '#f50057',
    },
  },
});
